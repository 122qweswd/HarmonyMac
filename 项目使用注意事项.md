# 建议运行前确认是否有下面事项未完成
其他模糊搜索相关文档位于record/下，项目使用调试记录位于openclaw-integration/下，插件使用方法通常在插件目录下

## 编译运行相关
git仓库存在部分bug，建议在拉取仓库后，使用压缩包版本文档中的Lib/覆盖仓库中版本
当前项目应当在MacOS中XCode 15及以上版本运行，编译MutualInfectionMac target
目前entitlement是手动赋予的，建议全局搜索jiahaoli并修改为使用者用户名！

### 针对.gitignore忽略的配置文件，下面提供模板，使用时需要替换其中信息。部分缺少的说明在相关文档中确认即可

openclaw配置文件（/NodeRuntimeHost/config/openclaw.template.json）模板位置：
qol/openclaw.template.json

xcode项目配置文件（MutualInfection.xcodeproj/xcshareddata/xcschemes/MutualInfectionMac.xcscheme）模板位置：
qol/MutualInfectionMac.xcscheme

## Openclaw相关
为正确编译Openclaw，openclaw-source中应该手动先完成依赖下载等
如果nodeRuntimeManager报告端口无法bind，请手动杀死openclaw的正在运行的进程
openclaw workspace中有默认记录完整chat，默认位置为/Users/{USERNAME}/Library/Containers/com.HarmonyOSInterconnection.app/Data/Library/Application Support/MutualInfectionMac/NodeRuntime/state/agents/main/sessions

### qol/a2a_chat.py
该文件可在openclaw正确启动后运行并用于本地与其交互，便于调试工具行为等。具体说明在文件内注释中

## nodejs runtime 相关
当前为支持arm64和x64架构，提供两个nodejs runtime来运行Openclaw，当前逻辑为在编译运行时进行解压缩，并进一步拉起openclaw运行。具体请确认MutualInfectionMac/Application/NodeRuntimeManager.swift确认。

