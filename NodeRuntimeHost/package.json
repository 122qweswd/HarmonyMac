{
  "name": "nshare-node-runtime-host",
  "version": "0.1.0",
  "private": true,
  "description": "MutualInfectionMac 内置 Node 运行时的最小宿主项目",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "node --watch dist/index.js"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
